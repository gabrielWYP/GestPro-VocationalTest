version: '3.8'

services:
  vocational-test:
    build:
      context: .
      dockerfile: Dockerfile
      # Plataformas soportadas (descomentar según necesidad)
      # platforms:
      #   - linux/amd64
      #   - linux/arm64
    ports:
      - "8000:8000"
    container_name: vocational-test-app
    restart: unless-stopped
    volumes:
      - vocational_data:/app/data
    environment:
      - FLASK_ENV=production
      - DATABASE_PATH=/app/data/vocational_test.db
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  vocational_data:
    driver: local
